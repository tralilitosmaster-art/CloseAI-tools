version: '3.8'

services:
  worker:
    build: .
    container_name: ozeraptor-worker
    env_file:
      - .env
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
